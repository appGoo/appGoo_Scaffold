#!/bin/bash
#
# This is the default shell script for preprocessing
# it is executed after the db has been confiormed as available
# and before any SQL is executed is prepared to be run the database
# note that all script files must be executable like
# chmod a+x
echo "preprocess.sh was run"
exit 0
